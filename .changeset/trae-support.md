---
"no-blind-coding": minor
---

Add Trae to `no-blind-coding-init` — generates `.trae/rules/project_rules.md`. (The MCP server already worked in Trae via `.trae/mcp.json`; this adds the native rules file.)
